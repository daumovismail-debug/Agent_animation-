# Основной бэкенд — расход с Pro/Max подписки.
# Требует один раз `claude login` через Claude Code CLI.
claude-agent-sdk>=0.1.0

# Fallback на случай отсутствия SDK подписки. Биллинг с ANTHROPIC_API_KEY.
anthropic>=0.40.0

# Telegram-бот
aiogram>=3.10.0
